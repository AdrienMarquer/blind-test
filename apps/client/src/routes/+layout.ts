// Disable server-side rendering for the entire app
// This makes it a true SPA (Single Page Application)
export const ssr = false;

// Prerender the index.html at build time
export const prerender = true;
