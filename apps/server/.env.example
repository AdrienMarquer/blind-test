# =============================================================================
# Blind Test - Development Environment Example
# =============================================================================
# Copy this file to .env and fill in your values

# Database (SQLite)
# Path to SQLite database file (will be created if it doesn't exist)
DATABASE_URL=./data/blind-test.db

# Server Configuration
NODE_ENV=development
PORT=3007

# Public URL (for audio streaming URLs sent to clients)
# In production, set this to your public domain (e.g., https://quiz.amrqr.fr)
# Leave empty or unset for local development (defaults to http://localhost:3007)
PUBLIC_URL=

# =============================================================================
# Metadata Enrichment Provider
# =============================================================================
# Choose which provider to use for music metadata enrichment
# Options: spotify, ai-openai, ai-anthropic, ai-google, ai-custom
METADATA_PROVIDER=spotify

# Fallback Provider (optional)
# If primary provider is missing critical fields (genre/album), fallback to this provider
# Set to same as METADATA_PROVIDER to disable fallback
# Options: spotify, ai-openai, ai-anthropic, ai-google, ai-custom
METADATA_FALLBACK_PROVIDER=ai-google

# Spotify API Credentials (if using spotify provider)
# Get these from: https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here

# OpenAI API (if using ai-openai provider)
# Get key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini

# Anthropic Claude API (if using ai-anthropic provider)
# Get key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# Google Gemini API (if using ai-google provider)
# Get key from: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here
GOOGLE_MODEL=gemini-2.0-flash-exp

# Upload Directory
UPLOAD_DIR=./uploads

# YouTube Download
# macOS (Homebrew): /opt/homebrew/bin/yt-dlp or /usr/local/bin/yt-dlp
# Linux: /usr/local/bin/yt-dlp or /usr/bin/yt-dlp
YTDLP_PATH=/opt/homebrew/bin/yt-dlp

# Logging
LOG_LEVEL=debug
